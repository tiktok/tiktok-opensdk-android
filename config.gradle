/**
 * 全局构建入口配置
 *
 * <p> 核心构建环境参数 $rootDir/buildcontext.gradle
 *
 */
//
//apply from: rootProject.file('buildcontext.gradle')
//
///**
// * 初始化全局构建参数上下文类
// * $rootDir/buildcontext.gradle
// */
//ext.BuildContext = initBuildContext()